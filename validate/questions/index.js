const { addQuestion } = require('./add');
const { editQuestion } = require('./edit');
const { getQuestionById } = require('./getById');

module.exports = {
    addQuestion,
    editQuestion,
    getQuestionById,
};
